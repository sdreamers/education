<?xml version="1.0" encoding="UTF-8"?>
<!--启动项设置为 trace，加载 springboot 启动内部各种详细输出-->
<Configuration status="INFO" monitorInterval="30">
    <Appenders>
        <!--添加一个控制台追加器-->
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level : %msg%n" />
        </Console>
        <!--添加一个文件追加器-->
        <RollingFile name="RollingFile" fileName="/mnt/logs/@activatedProperties@/dongpinyun"
                     filePattern="/mnt/logs/@activatedProperties@/dongpinyun.%d{yyyy-MM-dd}.log">
            <PatternLayout charset="UTF-8" pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} exec %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <!-- DefaultRolloverStrategy属性如不设置，则默认为最多同一文件夹下7个文件 -->
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>
    <Loggers>
        <!--根记录，调试模式，线上模式更改为INFO-->
        <Root level="INFO">
            <AppenderRef ref="Console" />
            <!-- AppenderRef 可以控制文件输出对象-->
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>
</Configuration>